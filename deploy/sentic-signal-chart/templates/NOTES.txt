1. Confirm CronJob created:
   kubectl get cronjob -n {{ .Release.Namespace }} {{ include "sentic-signal-chart.fullname" . }}

2. Watch Jobs spawned by this CronJob:
   kubectl get jobs -n {{ .Release.Namespace }} --watch

3. Check latest Pod logs for a run:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} --tail=100
